# Domain Docs

Before exploring, read `CONTEXT.md` and the relevant files in `docs/adr/`.

This is a single-context repository. Domain vocabulary comes from `CONTEXT.md`; ADRs record durable decisions in `docs/adr/`.

Use the glossary vocabulary in issue titles, specs, tests, and implementation notes. Flag conflicts with existing ADRs instead of silently overriding them.
